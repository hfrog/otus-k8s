dependencies:
  - name: redis-cart
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: cartservice
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: checkoutservice
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: emailservice
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: recommendationservice
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: frontend
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf

  - name: paymentservice
    version: "1.0.0"
    export-values:
      - parent: werf
        child: werf
